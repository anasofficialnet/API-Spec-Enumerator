export const DESIGN_CONFIG = {
    // Toggle the custom premium cursor on or off
    ENABLE_CUSTOM_CURSOR: false,

    // Base animations config (for reducing motion easily if needed programmatically, though CSS handles it too)
    ANIMATIONS: {
        enabled: true,
    }
};
